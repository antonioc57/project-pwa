{
  "name": "codigo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html && node generate-service-worker.js && cp ./src/web.config ./src/sitemap.xml ./src/robots.txt ./dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "workbox-build": "^4.3.1"
  },
  "dependencies": {
    "workbox-window": "^4.3.1"
  }
}
